# Copy to ~/.cargo/credentials.toml and fill in a real Artifactory access token.
# NOTE the "Bearer " prefix is required for JFrog.  Do NOT commit the real file.
[registries.artifactory]
token = "Bearer <YOUR_ARTIFACTORY_ACCESS_TOKEN>"
